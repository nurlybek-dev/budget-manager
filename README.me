# budget-manager
Implementation simple budget manager on django.

# Install
    git clone https://github.com/nurlybek-dev/budget-manager.git
    pipenv install
    or
    pip install -r requirements.txt

    rename .env-copy > .env
    and update it

    python manage.py migrate
    python manage.py runserver
